name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/12 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit Render backend
        run: |
          curl -I https://retail-market-behaviour.onrender.com

